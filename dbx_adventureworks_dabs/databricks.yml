# This is a Databricks asset bundle definition for dbx_adventureworks_dabs.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dbx_adventureworks_dabs
  uuid: 421f12f0-9e2f-41f5-8ad7-cdaa37697488

include:
  - resources/*.yml

sync:
  exclude:
    - .git/**
    - .gitignore
    - .vscode/**

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-4295250598855348.8.azuredatabricks.net

  prod:
    mode: production
    workspace:
      host: https://adb-4295250598855348.8.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/alex_gonzalez@hakkoda.io to make sure we only have a single copy.
      root_path: /Workspace/Users/alex_gonzalez@hakkoda.io/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: alex_gonzalez@hakkoda.io
        level: CAN_MANAGE
